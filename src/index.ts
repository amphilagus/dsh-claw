/**
 * `@deepseek-ai/dsh-claw` — the claw host bundle. The package's substance is
 * `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest field;
 * plugins load through subpath exports (`./home`, `./uv`, `./memory`).
 * @module @deepseek-ai/dsh-claw
 */

export {}
